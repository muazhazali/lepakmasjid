# Frontend (Vite)
VITE_APP_URL=http://localhost:8080
VITE_API_URL=/api

# Legacy — app no longer uses PocketBase SDK in production path
# VITE_POCKETBASE_URL=https://pb.muaz.app

# PocketBase → Postgres migration (server/scripts) — optional
# POCKETBASE_URL=https://pb.muaz.app
# POCKETBASE_ADMIN_EMAIL=
# POCKETBASE_ADMIN_PASSWORD=