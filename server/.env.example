# Copy to .env — or run `pnpm setup` from repo root (creates this for you)

DATABASE_URL=postgresql://lepakmasjid:lepakmasjid_dev@127.0.0.1:5432/lepakmasjid
JWT_SECRET=dev-only-change-in-production
PORT=3000
PUBLIC_URL=http://127.0.0.1:3000
UPLOAD_DIR=./uploads

SEED_ADMIN_EMAIL=admin@lepakmasjid.local
SEED_ADMIN_PASSWORD=adminadmin

# Optional: import from PocketBase (pnpm pb:export:public / pb:import:public)
# POCKETBASE_URL=https://pb.muaz.app
# POCKETBASE_ADMIN_EMAIL=
# POCKETBASE_ADMIN_PASSWORD=